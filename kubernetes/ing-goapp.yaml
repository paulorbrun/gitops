apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: goapp
spec:
  rules:
  - host: https://www.uol.com.br/
    http:
      paths:
      - backend:
          service:
            name: landing-page
            port:
              number: 8080
        path: /
        pathType: Exact
